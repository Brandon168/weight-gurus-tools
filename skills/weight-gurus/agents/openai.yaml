interface:
  display_name: "Weight Gurus"
  short_description: "Use for live Weight Gurus measurement pulls and JSON summaries."
  default_prompt: "Use weight-gurus-cli to fetch current Weight Gurus measurements as normalized JSON. Use weights aggregate only when I ask for summary data."
